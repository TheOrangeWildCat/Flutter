import 'package:flutter/material.dart';

class MultasScreen extends StatelessWidget {
  const MultasScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MultasScreen'),
      ),
    );
  }
}
